class Solution {
public:
    int largestElement(vector<int>& nums) {
        int length=nums.size();
     int largest=nums[0];
     for(int i=0; i<length; i++){
        if(nums[i]>largest){
            largest=nums[i];
        }
     }
     return largest;
    }
};
